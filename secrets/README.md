# Secrets Directory

Place non-versioned configuration such as API keys here.

## EXA MCP
- Copy this directory example to `exa.env`
- Export `EXA_API_KEY` before running Codex or source via shell profile

```
cp secrets/exa.env.example secrets/exa.env
```

Never commit populated secrets files.
